"""OSHIVA observability — operability of the agent.

  audit.py — append-only audit log of every turn (prompt, route, tool calls, answer, latency) and every
             feedback vote. Required for a confidential system: the incident-review / abuse-detection record.
"""
