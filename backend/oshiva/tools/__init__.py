"""OSHIVA tools — the functions an agent may call.

  registry.py — the OpenAI-style tool schemas + run_tool() dispatch. Mock data in the scaffold; wire to the
                real Infra / OCC / Service backends later (each still filtered by the caller's scopes).
"""
