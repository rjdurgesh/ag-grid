"""OSHIVA LLM layer — the model client.

  client.py — complete(): the single call the agent loop uses. A deterministic STUB brain now (no model
              needed); implement the real self-hosted GPT-OSS call later and the loop is unchanged.
"""
